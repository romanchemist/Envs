from setuptools import setup

setup(
    name='PrirodaOptimizer',    # название пакета
    version='0.1.0',
    packages=['conformer'],    # модули которые уст из пакета + 'prirodaoptimiser',
    zip_safe=False)    # распаковывать, прежде чем устанавливать
